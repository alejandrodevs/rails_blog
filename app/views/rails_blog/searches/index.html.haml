%h2= "Search results for ##{@search_criteria}"

- @posts.each do |post|
  .post-wrapper-condensed
    .row-fluid.post-wrapper-upper
      %h4.post-title
        = link_to post.title, custom_post_path(*post.url_params)
      %p.post-info
        = link_to post.author.name, post.author
        = post.published_at_description
    .post-content
      = preserve RailsBlog::Markdown.render(post.search_description).html_safe
